% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgm_amputate.R
\name{medsim_amputate}
\alias{medsim_amputate}
\title{Insert missing values under MCAR / MAR / MNAR (WS-B)}
\usage{
medsim_amputate(
  data,
  target,
  mechanism = c("MCAR", "MAR", "MNAR"),
  prop = 0.2,
  predictors = NULL,
  weights = NULL,
  type = c("RIGHT", "LEFT", "MID", "TAIL")
)
}
\arguments{
\item{data}{data.frame with columns \verb{X, M, Y} (+ optional covariates).}

\item{target}{Character vector of column(s) to set \code{NA} (e.g. \code{"M"}, or \code{c("M","Y")}).}

\item{mechanism}{One of \code{"MCAR"}, \code{"MAR"}, \code{"MNAR"}. MCAR: constant prob; MAR: logistic on
observed \code{predictors}; MNAR: logistic including the \code{target} itself (self-mechanism).}

\item{prop}{Target overall missingness proportion in \code{target}.}

\item{predictors}{Columns driving missingness for MAR/MNAR (defaults to all non-\code{target}).}

\item{weights}{Optional named weights for the missingness logistic.}

\item{type}{Amputation tail type, \code{mice::ampute}-style: one of \verb{"RIGHT","LEFT","MID","TAIL"}.}
}
\value{
\code{data} with \code{NA}s inserted in \code{target}; names/order preserved.
}
\description{
Thin, documented amputer aligning \code{mice::ampute} semantics to the medsim DGM contract.
Returns the input frame with \code{NA}s inserted in \code{target} column(s); column names/order
are preserved (DGM data contract).
}
