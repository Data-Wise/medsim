% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{medsim_write_submit_script}
\alias{medsim_write_submit_script}
\title{Write a SLURM array submit script for chunked simulation jobs}
\usage{
medsim_write_submit_script(
  config,
  run_script = "run_simulation_chunk.R",
  output_file = "submit_array.sh",
  account = NULL
)
}
\arguments{
\item{config}{A \code{medsim_config} object (from \code{\link[=medsim_config]{medsim_config()}}).  Must have
\code{n_chunks} > 0 and \code{mode == "cluster"}.}

\item{run_script}{Character: path (on the cluster) to the per-chunk R script
(the one that calls \code{\link[=medsim_run_chunk]{medsim_run_chunk()}}).  Default \code{"run_simulation_chunk.R"}.}

\item{output_file}{Character: where to write the bash script.  Default
\code{"submit_array.sh"} in the current directory.}

\item{account}{Character: SLURM account/PI account string (e.g. \code{"pi-dtofighi"}).
If \code{NULL}, no \code{--account} line is emitted.}
}
\value{
Invisibly, the path to the written script.
}
\description{
Generates a \code{submit_array.sh} bash script targeting the UNM CARC Hopper
cluster (and compatible SLURM systems).  The script submits one SLURM array
job per scenario chunk; each task runs an R script that calls
\code{\link[=medsim_run_chunk]{medsim_run_chunk()}}.
}
\examples{
cfg <- medsim_config(
  mode = "cluster", n_replications = 1000, n_chunks = 10,
  walltime = "04:00:00", mem_per_cpu = "8G"
)
tmp <- tempfile(fileext = ".sh")
medsim_write_submit_script(cfg, output_file = tmp)
# cat(readLines(tmp), sep = "\n")

}
\seealso{
\code{\link[=medsim_run_chunk]{medsim_run_chunk()}}, \code{\link[=medsim_combine_chunks]{medsim_combine_chunks()}}
}
