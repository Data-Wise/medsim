% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{.medsim_truth_fingerprint}
\alias{.medsim_truth_fingerprint}
\title{Content fingerprint of a scenario's truth inputs}
\usage{
.medsim_truth_fingerprint(scenario, truth_function)
}
\arguments{
\item{scenario}{A \code{medsim_scenario}.}

\item{truth_function}{The truth function passed to \code{medsim_run()}.}
}
\value{
A raw vector (compared with \code{identical()}).
}
\description{
Serialized identity of everything that determines a scenario's ground truth:
name, params (by value), and the \emph{deparsed source} of the data-generator and
the truth function. Used by \code{\link[=medsim_compute_all_truth]{medsim_compute_all_truth()}} to invalidate an
index-keyed truth cache on any content change (guards against stale-truth
reuse when an \code{output_dir} is reused after a scenario changes).

Deparse (source text) is used rather than serializing the live closures on
purpose: a closure's \code{serialize()} includes its whole enclosing environment,
which mutates as the caller adds unrelated bindings -- that would make the
fingerprint unstable across two identical calls and defeat legitimate cache
reuse. Deparse is environment-independent, and the \emph{values} a data-generator
depends on live in \code{params} (medsim convention), which is fingerprinted by
value. Residual limitation: a generator that closes over a value NOT in
\code{params} (e.g. \code{local({ m <- 5; function(n) rnorm(n, m) })}) with an identical
body and identical \code{params} would not be distinguished by a change to \code{m};
put such parameters in \code{params} for them to be tracked.
}
\keyword{internal}
