% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_gauge.R
\name{medsim_method_gauge}
\alias{medsim_method_gauge}
\title{Gauge-residual (P_med + W) estimator wrapper}
\usage{
medsim_method_gauge(
  data,
  params = list(),
  estimator = get0("ward_residual"),
  covars = "C",
  K = 5L,
  ci_level = 0.95,
  seed = 1L,
  reps = 1L,
  se_method = "analytic",
  B = 200L
)
}
\arguments{
\item{data}{data.frame with columns \code{A}, \code{M}, \code{Y}, \code{C}.}

\item{params}{Named list from \code{\link[=medsim_scenario_gauge]{medsim_scenario_gauge()}} (accepted for the
\code{method(data, params)} signature; unused by the estimator).}

\item{estimator}{Function; default \code{get0("ward_residual")}.}

\item{covars}{Character covariate names (default \code{"C"}).}

\item{K}{Integer cross-fitting folds (default 5).}

\item{ci_level}{Numeric CI level (default 0.95).}

\item{seed}{Integer seed (default 1).}

\item{reps}{Integer repeated cross-fitting fold draws (default 1).}

\item{se_method}{\code{"analytic"} (default) or \code{"bootstrap"} (percentile).}

\item{B}{Integer bootstrap resamples when \code{se_method = "bootstrap"} (default 200).}
}
\value{
Named list \verb{pmed, pmed_ci_lower, pmed_ci_upper, w, w_ci_lower, w_ci_upper}.
}
\description{
Adapter that calls an external \code{ward_residual()} estimator and returns the
flat field contract for two parameters:\tabular{ll}{
   Field \tab Description \cr
   \code{pmed}, \verb{pmed_ci_lower/_upper} \tab interventional proportion mediated \code{IIE/OE} \cr
   \code{w}, \verb{w_ci_lower/_upper} \tab gauge residual \code{R/OE} \cr
}


The estimator is \strong{not vendored} (medsim keeps estimators external). Pass it
via \code{estimator}, or default to an object named \code{ward_residual} on the search
path / \code{probmed::ward_residual}. The estimator must return an S7
\code{GaugePmedResult} (read via \verb{@p_med/@p_med_ci/@W/@W_ci}) or a plain list with
fields \code{p_med}, \code{p_med_ci} (length 2), \code{W}, \code{W_ci} (length 2).
}
\seealso{
\code{\link[=medsim_scenario_gauge]{medsim_scenario_gauge()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
