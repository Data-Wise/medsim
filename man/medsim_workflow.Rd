% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/workflow.R
\name{medsim_workflow}
\alias{medsim_workflow}
\title{Generate analysis, figures, and tables from simulation results}
\usage{
medsim_workflow(
  results,
  output_dir = "results",
  figures_format = "pdf",
  tables_format = "latex",
  ...
)
}
\arguments{
\item{results}{A \code{medsim_results} object from \code{\link[=medsim_run]{medsim_run()}}.}

\item{output_dir}{Top-level directory for all outputs. Subdirectories
\verb{figures/} and \verb{tables/} are created underneath. Created if it doesn't exist.}

\item{figures_format}{File format for figures (e.g. \code{"pdf"}, \code{"png"}).}

\item{tables_format}{Output format for tables (e.g. \code{"latex"}, \code{"markdown"}).}

\item{...}{Additional arguments passed to \code{\link[=medsim_figures]{medsim_figures()}}.}
}
\value{
Invisibly, a list with elements \code{analysis}, \code{figures} (file paths),
and \code{tables} (return value of \code{\link[=medsim_tables_workflow]{medsim_tables_workflow()}}).
}
\description{
End-to-end output pipeline: runs \code{\link[=medsim_analyze]{medsim_analyze()}}, \code{\link[=medsim_figures]{medsim_figures()}},
and \code{\link[=medsim_tables]{medsim_tables()}} and writes everything to \code{output_dir}.
}
\examples{
\dontrun{
config <- medsim_config("test")
scenarios <- medsim_scenarios_mediation()
results <- medsim_run(my_method, scenarios, config)
medsim_workflow(results, output_dir = "results")
}
}
