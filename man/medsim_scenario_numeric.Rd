% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios_numeric.R
\name{medsim_scenario_numeric}
\alias{medsim_scenario_numeric}
\title{Create a numeric accuracy simulation scenario}
\usage{
medsim_scenario_numeric(
  name,
  true_params = list(),
  data_generator = NULL,
  extra = character()
)
}
\arguments{
\item{name}{Character: scenario name.}

\item{true_params}{Named list of ground-truth parameter values.  These are
passed to \code{method(data, params)} as \code{params}; what counts as "truth" is
method-defined.  For accuracy studies this typically includes the analytic
reference value (e.g. \code{list(ci_true = 0.95)}).}

\item{data_generator}{Function \verb{function(n)} returning a data frame.
Defaults to a no-op that returns an empty data frame (suitable for methods
that derive everything from \code{params} rather than a random sample).}

\item{extra}{Character vector of additional result columns beyond the
mandatory \code{error}/\code{abs_error}/\code{elapsed_sec} columns.  These are passed to
\code{\link[=medsim_estimand]{medsim_estimand()}} and stored on the scenario.}
}
\value{
A \code{medsim_scenario} object with \code{estimand$kind = "numeric"}.
}
\description{
Thin wrapper over \code{\link[=medsim_scenario]{medsim_scenario()}} for studies that evaluate numerical
\strong{accuracy} or \strong{timing} of a computation — not coverage or power.  The
estimand kind is \code{"numeric"}, which signals downstream commands to skip
coverage/power analysis and produce accuracy/timing tables instead.

Typical use-cases:
\itemize{
\item Product-of-three CI: compare DOP, MBCO, Monte-Carlo CI point accuracy
and computation time across parameter cells.
\item Approximation quality: absolute error and relative error vs. analytic truth.
}

The \code{data_generator} for numeric scenarios is optional (default: returns
\code{data.frame()} with \code{n} rows of \code{NA}s as a placeholder).  Methods that
only need \code{params} can ignore the \code{data} argument.
}
\examples{
# Product-of-three scenario cell
sc <- medsim_scenario_numeric(
  name        = "dop_small_n",
  true_params = list(a  = 0.5, b = 0.3, se_a = 0.1, se_b = 0.1,
                     ci_true = 0.01),
  extra       = c("elapsed_sec")
)
str(sc$estimand)

}
\seealso{
\code{\link[=medsim_estimand]{medsim_estimand()}}, \code{\link[=medsim_scenario]{medsim_scenario()}}, \code{\link[=medsim_analyze]{medsim_analyze()}}
}
