% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{medsim_compare_methods}
\alias{medsim_compare_methods}
\title{Compare Multiple Methods}
\usage{
medsim_compare_methods(..., metrics = "all")
}
\arguments{
\item{...}{Named medsim_results objects (e.g., method1 = results1,
method2 = results2)}

\item{metrics}{Character vector: which comparison metrics to compute.
Options: "accuracy", "timing", "coverage", "all" (default)}
}
\value{
A list with class "medsim_comparison" containing:
\itemize{
\item \code{accuracy_comparison}: data.frame comparing accuracy metrics
\item \code{timing_comparison}: data.frame comparing timing
\item \code{coverage_comparison}: data.frame comparing coverage (if applicable)
\item \code{summary}: Overall comparison summary
}
}
\description{
Compares performance of multiple estimation methods across scenarios.
Combines results from multiple simulation runs and computes relative
performance metrics.
}
\details{
\subsection{Comparison Metrics}{
\itemize{
\item \strong{Accuracy}: MAE, RMSE, bias across methods
\item \strong{Timing}: Mean/median execution time
\item \strong{Coverage}: CI coverage rates (if CIs available)
\item \strong{Relative Efficiency}: Ratio of MSE (lower = better)
}
}

\subsection{Notes}{

All results must:
\itemize{
\item Use the same scenarios (matched by name)
\item Use the same configuration (n_replications, seed)
\item Have ground truth available (for accuracy comparison)
}
}
}
\examples{
\dontrun{
# Run multiple methods
results_proposed <- medsim_run(method_proposed, scenarios, config)
results_delta <- medsim_run(method_delta, scenarios, config)
results_boot <- medsim_run(method_boot, scenarios, config)

# Compare
comparison <- medsim_compare_methods(
  proposed = results_proposed,
  delta = results_delta,
  bootstrap = results_boot
)

# View accuracy comparison
print(comparison$accuracy_comparison)

# View timing comparison
print(comparison$timing_comparison)
}

}
\seealso{
\code{\link[=medsim_analyze]{medsim_analyze()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
