% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze.R
\name{medsim_analyze_power}
\alias{medsim_analyze_power}
\title{Analyze Statistical Power}
\usage{
medsim_analyze_power(
  results,
  alpha = 0.05,
  p_suffix = "_p",
  null_value = 0,
  by_scenario = TRUE
)
}
\arguments{
\item{results}{A medsim_results object from \code{\link[=medsim_run]{medsim_run()}}}

\item{alpha}{Numeric: significance level (default: 0.05)}

\item{p_suffix}{Character: suffix for p-value columns (default: "_p")
E.g., if parameter is "indirect", looks for "indirect_p"}

\item{null_value}{Numeric: null hypothesis value (default: 0)}

\item{by_scenario}{Logical: Compute power separately for each scenario
(default TRUE)}
}
\value{
A list with class "medsim_power" containing:
\itemize{
\item \code{power}: data.frame with power rates
\item \code{by_scenario}: data.frame with power by scenario (if by_scenario=TRUE)
\item \code{summary}: Overall summary statistics
}
}
\description{
Computes empirical power (rejection rate under alternative hypothesis) from
simulation results. Requires that the simulation method returns p-values.
}
\details{
\subsection{Power Interpretation}{

Power is the probability of correctly rejecting the null hypothesis when
it is false (i.e., detecting a true effect).
\itemize{
\item Power = proportion of replications where p < alpha
\item Higher power = better ability to detect effects
\item Power depends on: effect size, sample size, alpha level
}
}

\subsection{Notes}{

This function computes \strong{empirical power} from simulations, not theoretical
power. For scenarios where the null is true (e.g., indirect effect = 0),
the rejection rate represents Type I error rate, not power.
}
}
\examples{
\dontrun{
# Method that returns p-values
my_method <- function(data, params) {
  # ... estimation and testing
  list(
    indirect = estimate,
    indirect_p = p_value
  )
}

results <- medsim_run(my_method, scenarios, config)

# Analyze power
power <- medsim_analyze_power(results, alpha = 0.05)

print(power$power)
#>   parameter  power    n_valid
#>   indirect   0.847    1000
}

}
\seealso{
\code{\link[=medsim_analyze]{medsim_analyze()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
