% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{.medsim_pilot_check}
\alias{.medsim_pilot_check}
\title{Pilot-subset positive control (Gate D)}
\usage{
.medsim_pilot_check(combined, pilot_reference, pilot_tol = 1e-09)
}
\arguments{
\item{combined}{The combined \code{medsim_results} object.}

\item{pilot_reference}{Path to a pilot RDS, or a \code{medsim_results} object.}

\item{pilot_tol}{Absolute tolerance for value agreement.}
}
\value{
List of violations (empty if the control passes).
}
\description{
Asserts a full run's replications \verb{1..B_pilot} reproduce an archived pilot:
(1) IDENTITY -- same sample size \code{n} and same scenario fingerprints, so a
stale/mis-configured pilot fails as \code{pilot_config_differs} rather than
masquerading as a seeding regression (the seed ignores \code{n}: same rep id at
a different \code{n} draws different-length data on CORRECT code); then
(2) VALUES -- estimate columns only (never \code{elapsed}/metadata), joined on
\verb{(scenario, replication)}, within \code{pilot_tol}.
}
\keyword{internal}
