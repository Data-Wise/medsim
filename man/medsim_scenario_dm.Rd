% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios_dm.R
\name{medsim_scenario_dm}
\alias{medsim_scenario_dm}
\title{Construct a differential-misclassification (DM) mediation scenario}
\usage{
medsim_scenario_dm(
  name,
  true_params,
  dm_params = list(),
  misclass_type = c("mediator", "exposure")
)
}
\arguments{
\item{name}{Scenario name (character).}

\item{true_params}{List of true generating parameters passed through to
\code{medrobust::simulate_dm_data()} and stored in \code{scenario$params}.
Must include at minimum \code{NDE} and \code{NIE} (the true natural direct/indirect
effects) so that coverage against ground truth can be computed.}

\item{dm_params}{List of differential-misclassification parameters passed
through to \code{medrobust::simulate_dm_data()}.}

\item{misclass_type}{\code{"mediator"} (default, me-mediator-bounds study) or
\code{"exposure"} (me-exposure-recall study).}
}
\value{
A \code{\link[=medsim_scenario]{medsim_scenario()}} object with
\code{estimand = medsim_estimand("interval", params = c("NDE", "NIE"), ...)}.
}
\description{
Builds a \code{\link[=medsim_scenario]{medsim_scenario()}} for the bounds-pair simulation studies
(me-mediator-bounds / me-exposure-recall).  The \code{data_generator} wraps
\code{medrobust::simulate_dm_data(...)@observed}; if \pkg{medrobust} is not
installed a lightweight synthetic fallback is used so that the harness and
\code{interval}-kind analysis machinery can be unit-tested without the package.

The scenario is tagged with \code{estimand = medsim_estimand("interval", ...)} so
that \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} automatically dispatches the
Imbens-Manski / partial-ID interval branch.
}
\examples{
# Synthetic scenario (no medrobust required):
sc <- medsim_scenario_dm(
  name        = "small_misclass",
  true_params = list(NDE = 0.2, NIE = 0.3, n = 200),
  dm_params   = list(delta = 0.1),
  misclass_type = "mediator"
)
# Data generator uses synthetic fallback when medrobust is absent:
d <- sc$data_generator(n = 50)
head(d)

}
\seealso{
\code{\link[=medsim_method_bounds]{medsim_method_bounds()}} for the corresponding estimator adapter;
\code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} for interval-kind coverage analysis.
}
