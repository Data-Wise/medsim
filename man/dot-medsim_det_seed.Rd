% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{.medsim_det_seed}
\alias{.medsim_det_seed}
\title{Deterministic Per-Replication Seed}
\usage{
.medsim_det_seed(scenario_name, rep_id)
}
\arguments{
\item{scenario_name}{Character: scenario name.}

\item{rep_id}{Integer: \strong{global} replication id (i.e. already offset by
\code{config$rep_offset} for chunked runs -- see \code{\link[=medsim_run_single_replication]{medsim_run_single_replication()}}).}
}
\value{
Integer in \verb{[1, .Machine$integer.max]}.
}
\description{
Derives a seed from \verb{(scenario_name, rep_id)} alone, independent of chunk
count, worker count, cluster type, or execution order. This is what makes
\code{\link[=medsim_run_chunk]{medsim_run_chunk()}} safe to split across any number of SLURM array tasks:
replication \code{k} of a given scenario draws the same data regardless of
which chunk or worker happens to process it.
}
\keyword{internal}
