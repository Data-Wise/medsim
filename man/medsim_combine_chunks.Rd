% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{medsim_combine_chunks}
\alias{medsim_combine_chunks}
\title{Combine chunk RDS files produced by medsim_run_chunk()}
\usage{
medsim_combine_chunks(
  output_dir,
  pattern = "chunk_*.rds",
  expected_chunks = NULL,
  on_violation = c("stop", "warn", "ignore"),
  nsim = NULL,
  collapse_threshold = 0.9,
  collapse_digits = 12L,
  collapse_min_cell = 30L,
  collapse_exclude = .medsim_collapse_exclude_default,
  pilot_reference = NULL,
  pilot_tol = 1e-09,
  verbose = TRUE
)
}
\arguments{
\item{output_dir}{Character: directory containing \code{chunk_*.rds} files.}

\item{pattern}{Character: glob pattern for chunk files.
Default \code{"chunk_*.rds"}.}

\item{expected_chunks}{Integer or \code{NULL}: if supplied (e.g. the SLURM array
size), a shortfall in chunk files is reported as an integrity violation
(see \code{on_violation}) -- catching a failed/timed-out task instead of
silently combining a partial grid. Default \code{NULL} (no check).}

\item{on_violation}{One of \code{"stop"} (default), \code{"warn"}, \code{"ignore"}: what to
do when an integrity violation is found (missing chunks, duplicate or
gapped replication ids, ragged cells, output collapse, all-failed cells,
cross-scenario seed collisions). \code{"stop"} signals a
\code{medsim_combine_violation} condition that CARRIES the combined results --
recover an hours-long run's good cells with
\code{tryCatch(medsim_combine_chunks(...), medsim_combine_violation = function(e) e$results)}.
For a deliberate partial combine (an interim look at a still-running
array), pass \code{on_violation = "warn"}.}

\item{nsim}{Integer or \code{NULL}: if supplied, additionally assert that every
scenario has exactly \code{nsim} replications (the contiguity audit is
otherwise self-validating and needs no external count).}

\item{collapse_threshold, collapse_digits, collapse_min_cell}{Tuning for the
collapse-signature audit: per scenario and per continuous estimate column,
require \code{n_distinct(round(x, collapse_digits)) > collapse_threshold * n_ok}
(defaults 0.9 and 12; calibration: the 0.3.1 seed-collapse produced ~17
distinct outcomes in 1000). Cells with fewer than \code{collapse_min_cell}
non-NA values (default 30) are skipped as too small to diagnose.}

\item{collapse_exclude}{Character: DISCRETE method-contract fields excluded
from the collapse audit BY NAME (default: \code{converged}, \code{branch_switch},
plus the \code{medsim_method_mbco_mi()} branch diagnostics \code{branch_mix},
\code{stacked_branch}, \code{p_branch_a}, \code{r4}, \code{r4_fixed} -- 0/1 flags, an
\code{m}-valued share, and ARIVs with point mass at 0, all legitimately
low-cardinality). Name-based (not distinctness-based) so a totally
collapsed estimate column cannot exempt itself; add any custom discrete
field your method emits.}

\item{pilot_reference}{Character path (or \code{medsim_results} object): an
archived pilot run to use as a positive control (Gate D). Because seeds
depend only on \verb{(scenario, replication)}, the full run's replications
\verb{1..B_pilot} are draw-identical to the pilot -- a free regression check
that harness, environment, and seeding are unchanged since the pilot
passed. Identity is asserted FIRST (same sample size \code{n}, same scenario
generator/params via fingerprint) so a stale or mis-configured pilot
fails loud as \code{pilot_config_differs} instead of masquerading as a
seeding regression; then estimate columns (only -- never \code{elapsed} or
other metadata) are compared within \code{pilot_tol}. Default \code{NULL} (no
pilot check).}

\item{pilot_tol}{Numeric: absolute per-value tolerance for the pilot
comparison (default \code{1e-9}). Byte-equality is deliberately NOT required:
a different BLAS/R build can legitimately differ in low-order bits on
correct code (FORK-reproducibility scope). For a rebuilt environment,
loosen the tolerance or use \code{on_violation = "warn"}.}

\item{verbose}{Logical: print file counts.}
}
\value{
A \code{medsim_results} object with combined \verb{$results} and \verb{$truth}.
}
\description{
Reads all \verb{chunk_<k>.rds} files from \code{output_dir}, merges their \verb{$results}
and \verb{$truth} data frames, and returns a single \code{medsim_results} object
that is equivalent to running all replications in one call.
}
\examples{
# After all SLURM chunks complete:
# combined <- medsim_combine_chunks("simulation_results/")
# coverage <- medsim_analyze_coverage(combined)

}
\seealso{
\code{\link[=medsim_run_chunk]{medsim_run_chunk()}}, \code{\link[=medsim_audit_results]{medsim_audit_results()}},
\code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
