% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_sobol.R
\name{medsim_method_sobol}
\alias{medsim_method_sobol}
\title{Sobol / functional-ANOVA variance-share estimator wrapper}
\usage{
medsim_method_sobol(
  data,
  params = list(),
  estimator = get0("sobol_pmed"),
  covars = "C",
  K = 5L,
  level = 0.95,
  seed = 1L
)
}
\arguments{
\item{data}{A \code{data.frame} with columns \code{A}, \code{M}, \code{Y}, \code{C}.}

\item{params}{Named list from \code{\link[=medsim_scenario_sobol]{medsim_scenario_sobol()}} (unused by the
estimator itself, but accepted for the standard \code{method(data, params)}
signature; may carry \code{covars}, \code{K}, \code{level}).}

\item{estimator}{Function implementing the Sobol estimator.  Defaults to
\code{get0("sobol_pmed")}, i.e. an object named \code{sobol_pmed} visible on the
search path (e.g. after \code{source()}-ing the prototype).}

\item{covars}{Character vector of covariate column names passed to the
estimator (default \code{"C"}).}

\item{K}{Integer cross-fitting folds passed to the estimator (default 5).}

\item{level}{Numeric CI level passed to the estimator (default 0.95).}

\item{seed}{Integer seed passed to the estimator (default 1).}
}
\value{
A named list with fields \code{pmed_sobol}, \code{pmed_sobol_ci_lower},
\code{pmed_sobol_ci_upper}.
}
\description{
Adapter that calls an external \code{sobol_pmed()} estimator and returns the flat
field contract keyed by the \code{pmed_sobol} token:\tabular{ll}{
   Field \tab Description \cr
   \code{pmed_sobol} \tab Point estimate of the Sobol share \eqn{V_{med}/V_T} \cr
   \code{pmed_sobol_ci_lower} \tab Lower Wald CI bound \cr
   \code{pmed_sobol_ci_upper} \tab Upper Wald CI bound \cr
}


Following medsim convention, the actual estimator is \strong{not vendored} into
the package (medsim keeps estimators external — it \code{Suggests} companion
packages, never hard-depends on them).  The prototype lives at
\code{pmed-modern-sobol/03-sobol-pmed/sims/sobol_pmed.R}; \code{source()} it (or load
the future \code{probmed} export) before running, or pass it via \code{estimator}.

The external \code{sobol_pmed(d, ...)} is expected to return a list with
\code{P_med_sobol} (scalar) and \code{ci} (length-2 numeric \code{c(lower, upper)}).
}
\seealso{
\code{\link[=medsim_scenario_sobol]{medsim_scenario_sobol()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
