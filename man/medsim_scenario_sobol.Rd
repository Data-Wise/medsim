% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios_sobol.R
\name{medsim_scenario_sobol}
\alias{medsim_scenario_sobol}
\title{Create a Sobol / functional-ANOVA variance-share simulation scenario}
\usage{
medsim_scenario_sobol(name, true_params = list(), pd = 0.5, pm = 0.5)
}
\arguments{
\item{name}{Character: scenario name passed to \code{\link[=medsim_scenario]{medsim_scenario()}}.}

\item{true_params}{Named list with entries (defaults in parentheses):
\itemize{
\item \code{beta_a}: path coefficient A -> M (0.6)
\item \code{tau_a}: direct path A -> Y (0.5)
\item \code{tau_m}: path coefficient M -> Y (0.7)
\item \code{kappa}: A*M interaction coefficient in the Y model (0.0)
\item \code{gamma_mc}: covariate effect C -> M (0.5)
\item \code{gamma_yc}: covariate effect C -> Y (0.4)
\item \code{p_a}: treatment-assignment probability used for the variance weights
\code{pd = pm = p_a} (0.5)
}}

\item{pd, pm}{Bernoulli weights for the direct / mediator design variances in
the functional-ANOVA decomposition (default \code{0.5}; override only for
non-balanced designs).}
}
\value{
A \code{medsim_scenario} object with \code{estimand$kind = "variance_share"}
and \code{params$pmed_sobol} holding the closed-form ground truth.
}
\description{
Thin wrapper over \code{\link[=medsim_scenario]{medsim_scenario()}} that bakes in:
\itemize{
\item A linear-Gaussian-with-interaction data generator for the
\code{A -> M -> Y} causal chain with a single covariate \code{C} and an \code{A*M}
interaction term (\code{kappa}).
\item An \code{estimand = medsim_estimand("variance_share", params = "pmed_sobol", ci = "standard")} descriptor, so \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} uses the
generic Wald \code{truth \%in\% CI} path and \code{\link[=medsim_validate_scenario]{medsim_validate_scenario()}} checks
for the \code{A/M/Y/C} columns the Sobol estimator requires.
\item A \strong{closed-form} \code{truth} for the Sobol share, exact for the
linear-Gaussian-with-interaction DGP (no Monte-Carlo PO draw needed).
}

The estimand is the variance-scale proportion mediated
\eqn{P_{med}^{\sigma^2} = V_{med}/V_T}, where the functional-ANOVA variance
components are built from the four corner means
\eqn{\theta_{a,a'} = E[Y(a, M(a'))]}.  For the linear-Gaussian DGP with
centered \code{C},
\deqn{\theta_{a,a'} = \tau_a a + (\tau_m + \kappa a)\,\beta_a a'.}
At \code{kappa = 0} the share reduces to \eqn{NIE^2/(NIE^2 + NDE^2)} with
\eqn{NIE = \tau_m \beta_a} and \eqn{NDE = \tau_a}.
}
\examples{
sc <- medsim_scenario_sobol(
  name        = "interaction",
  true_params = list(beta_a = 0.6, tau_a = 0.5, tau_m = 0.7, kappa = 0.4)
)
sc$params$pmed_sobol   # closed-form Sobol share

}
\seealso{
\code{\link[=medsim_method_sobol]{medsim_method_sobol()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}},
\code{\link[=medsim_estimand]{medsim_estimand()}}
}
