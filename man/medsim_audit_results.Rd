% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{medsim_audit_results}
\alias{medsim_audit_results}
\title{Audit a combined results object for chunked-run integrity violations}
\usage{
medsim_audit_results(
  results,
  on_violation = c("stop", "warn", "ignore"),
  nsim = NULL,
  collapse_threshold = 0.9,
  collapse_digits = 12L,
  collapse_min_cell = 30L
)
}
\arguments{
\item{results}{A \code{medsim_results} object (e.g. from
\code{\link[=medsim_combine_chunks]{medsim_combine_chunks()}}), or a bare results data.frame.}

\item{on_violation}{One of \code{"stop"} (default), \code{"warn"}, \code{"ignore"}: what to
do when an integrity violation is found (missing chunks, duplicate or
gapped replication ids, ragged cells, output collapse, all-failed cells,
cross-scenario seed collisions). \code{"stop"} signals a
\code{medsim_combine_violation} condition that CARRIES the combined results --
recover an hours-long run's good cells with
\code{tryCatch(medsim_combine_chunks(...), medsim_combine_violation = function(e) e$results)}.
For a deliberate partial combine (an interim look at a still-running
array), pass \code{on_violation = "warn"}.}

\item{nsim}{Integer or \code{NULL}: if supplied, additionally assert that every
scenario has exactly \code{nsim} replications (the contiguity audit is
otherwise self-validating and needs no external count).}

\item{collapse_threshold, collapse_digits, collapse_min_cell}{Tuning for the
collapse-signature audit: per scenario and per continuous estimate column,
require \code{n_distinct(round(x, collapse_digits)) > collapse_threshold * n_ok}
(defaults 0.9 and 12; calibration: the 0.3.1 seed-collapse produced ~17
distinct outcomes in 1000). Cells with fewer than \code{collapse_min_cell}
non-NA values (default 30) are skipped as too small to diagnose.}
}
\value{
Invisibly, the list of violations found (empty if clean). Signalling
follows \code{on_violation}, exactly as in \code{\link[=medsim_combine_chunks]{medsim_combine_chunks()}}.
}
\description{
Standalone entry point for Gate A of the chunked-run integrity layer
(SPEC-medsim-chunked-run-gates-2026-07-31): run the same audit
\code{\link[=medsim_combine_chunks]{medsim_combine_chunks()}} performs, on an already-combined
\code{medsim_results} object. Checks, per scenario:
\enumerate{
\item \strong{Contiguity} -- \code{replication} ids form a gapless, duplicate-free
\verb{1..max} run, with the same \code{max} in every scenario (self-validating:
needs no external replication count).
\item \strong{Collapse signature} -- every continuous estimate column has more than
\code{collapse_threshold * n_ok} distinct values (the 0.3.1 seed-collapse
produced ~17 distinct outcomes in 1000 while every chunk exited 0).
An all-failed cell (\code{n_ok == 0}) is reported as \code{cell_failed}, not
collapse.
\item \strong{Seed-space collisions} -- no two scenarios map onto overlapping
\code{\link[=.medsim_det_seed]{.medsim_det_seed()}} sequences (the ~1e6-bucket name hash can collide).
}

Frames without the schema-v2 stamp (produced before medsim 0.5.0, when
\code{replication} was chunk-LOCAL and collided across chunks) skip checks 1
and 3 with a warning -- they cannot be audited for id integrity.
}
\seealso{
\code{\link[=medsim_combine_chunks]{medsim_combine_chunks()}}
}
