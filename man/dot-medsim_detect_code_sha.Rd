% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{.medsim_detect_code_sha}
\alias{.medsim_detect_code_sha}
\title{Auto-detect a code-state identifier for chunk provenance (Gate C)}
\usage{
.medsim_detect_code_sha()
}
\value{
Character scalar: a 40-hex SHA or a \verb{pkg:medsim-<version>} tag.
}
\description{
\verb{git rev-parse HEAD} in the running script's directory (from
\verb{commandArgs(--file=)}, falling back to \code{getwd()} for interactive use --
the RUN SCRIPT's tree, not wherever R happened to start). Outside a git
tree, degrades to a \verb{pkg:medsim-<version>} tag: stable across chunks of one
submission (so the combine assertion stays quiet) but unable to distinguish
code states -- true mid-run-edit detection needs git.
}
\keyword{internal}
