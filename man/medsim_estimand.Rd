% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimand.R
\name{medsim_estimand}
\alias{medsim_estimand}
\title{Declare the estimand kind for a simulation scenario}
\usage{
medsim_estimand(
  kind = c("point", "interval", "probabilistic", "variance_share", "numeric"),
  params = character(),
  truth = NULL,
  ci = c("standard", "imbens_manski", "mbco", "none"),
  extra = character()
)
}
\arguments{
\item{kind}{Estimand kind: \code{"point"} (default), \code{"interval"},
\code{"probabilistic"}, \code{"variance_share"}, or \code{"numeric"}.}

\item{params}{Character vector of estimand parameter names -- e.g.
\code{c("indirect")} for point, \code{c("NDE", "NIE")} for interval,
\code{c("pmed")} for probabilistic.  Defaults to \code{character()} (auto-inferred
downstream).}

\item{truth}{Optional truth-extractor \verb{function(scenario)} returning a
\strong{named} numeric vector keyed by the names in \code{params}.  Required for
\code{kind = "interval"} (bounds truth differs from the estimand itself).}

\item{ci}{CI method: \code{"standard"} (Wald/bootstrap), \code{"imbens_manski"}
(partial-ID dual CI), \code{"mbco"} (constrained optimisation), or \code{"none"}.}

\item{extra}{Character vector of additional result columns beyond \code{params}
that \code{method()} returns -- e.g. \code{c("feasible", "branch_switch", "timing")}.}
}
\value{
An object of class \code{c("medsim_estimand", "list")}.
}
\description{
Attach a first-class estimand descriptor to a \code{\link[=medsim_scenario]{medsim_scenario()}}.  Every
downstream command (\code{medsim_run}, \code{medsim_analyze_coverage},
\code{medsim_tables}, \code{medsim_figures}, \code{medsim_workflow}) dispatches on
\code{estimand$kind} rather than assuming a scalar point-estimand.

Five kinds are recognised:\tabular{lll}{
   kind \tab what \code{method()} returns \tab coverage notion \cr
   \code{"point"} \tab \code{{p}}, \verb{\{p\}_ci_lower/_upper}, \verb{\{p\}_p} \tab truth in CI \cr
   \code{"interval"} \tab \verb{\{p\}_lower/_upper}, \verb{\{p\}_im_lower/_im_upper}, \code{feasible}, \code{falsified} \tab truth in [lower, upper]; IM-CI coverage \cr
   \code{"probabilistic"} \tab \code{pmed}, \verb{pmed_ci_lower/_upper}, \code{pmed_p}, \code{branch_switch} \tab truth in CI (truth from potential outcomes) \cr
   \code{"variance_share"} \tab \code{{p}}, \verb{\{p\}_ci_lower/_upper} \tab truth in Wald CI (bounded scalar share in [0,1]) \cr
   \code{"numeric"} \tab \code{error}, \code{abs_error}, \code{elapsed_sec} \tab none \cr
}


\code{"variance_share"} is a bounded scalar point estimand with a standard Wald
CI — the Sobol / functional-ANOVA proportion mediated
\eqn{P_{med}^{\sigma^2} = V_{med}/V_T \in [0,1]} (see
\code{\link[=medsim_scenario_sobol]{medsim_scenario_sobol()}}, \code{\link[=medsim_method_sobol]{medsim_method_sobol()}}).  Coverage uses the
generic \verb{truth in CI} path; the dedicated kind exists for clear labeling and
so \code{\link[=medsim_validate_scenario]{medsim_validate_scenario()}} checks the causal-notation \code{A/M/Y/C} columns
the Sobol estimator requires (rather than the legacy \code{X/M/Y} of \code{"point"}).

Scenarios with \code{estimand = NULL} (the default in \code{\link[=medsim_scenario]{medsim_scenario()}}) are
treated as \code{kind = "point"} throughout the package -- full back-compatibility
with all existing code.
}
\examples{
# Point estimand (the historic default -- back-compat)
medsim_estimand("point")

# Interval (partial-ID bounds)
medsim_estimand("interval",
  params = c("NDE", "NIE"),
  ci     = "imbens_manski",
  extra  = c("feasible", "falsified"))

# Probabilistic (P_med)
medsim_estimand("probabilistic",
  params = "pmed",
  ci     = "mbco",
  extra  = "branch_switch")

# Numerical accuracy (no coverage/power)
medsim_estimand("numeric", params = "error", ci = "none",
  extra = "elapsed_sec")

}
\seealso{
\code{\link[=medsim_scenario]{medsim_scenario()}} for attaching an estimand to a scenario;
\code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} for the keystone dispatch on kind.
}
