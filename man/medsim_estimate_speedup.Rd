% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{medsim_estimate_speedup}
\alias{medsim_estimate_speedup}
\title{Estimate Parallel Speedup}
\usage{
medsim_estimate_speedup(n_tasks, n_cores, overhead = 0.1)
}
\arguments{
\item{n_tasks}{Integer: Number of tasks}

\item{n_cores}{Integer: Number of cores}

\item{overhead}{Numeric: Overhead per core (default 0.1 = 10\%)}
}
\value{
Named list with:
\itemize{
\item \code{speedup}: Expected speedup factor
\item \code{efficiency}: Parallel efficiency (0-1)
\item \code{recommendation}: Character recommendation
}
}
\description{
Estimates the expected speedup from parallel processing based on number of
cores and task characteristics.
}
\details{
Speedup is estimated using Amdahl's law with overhead:

Speedup = n_cores / (1 + overhead * n_cores)

Efficiency = Speedup / n_cores
}
\examples{
# For 100 tasks on 10 cores
est <- medsim_estimate_speedup(100, 10)
cat(sprintf("Expected speedup: \%.1fx\n", est$speedup))
cat(sprintf("Efficiency: \%.0f\%\%\n", est$efficiency * 100))

}
