% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_pmed.R
\name{medsim_method_pmed_mbco}
\alias{medsim_method_pmed_mbco}
\title{MBCO confidence interval for P_med (two-branch method)}
\usage{
medsim_method_pmed_mbco(data, params, alpha = 0.05, n_boot = 2000L)
}
\arguments{
\item{data}{A \code{data.frame} with columns \code{A}, \code{M}, \code{Y}.}

\item{params}{Named list from \code{\link[=medsim_scenario_pmed]{medsim_scenario_pmed()}}; must contain at
minimum \code{alpha_ax}, \code{beta_my}, \code{beta_ay}, \code{sigma_m}, \code{sigma_y}.}

\item{alpha}{Significance level for CI (default 0.05).}

\item{n_boot}{Integer: parametric bootstrap size for P_med point estimate
(default 2000).}
}
\value{
A named list with fields \code{pmed}, \code{pmed_ci_lower}, \code{pmed_ci_upper},
\code{pmed_p}, \code{branch_switch}, \code{converged}.
}
\description{
Estimates P_med from observed data using OLS point estimation and a
\strong{two-branch MBCO} (maximally balanced constrained optimisation) confidence
interval.  Returns the 6-field contract expected by \code{\link[=medsim_run]{medsim_run()}}:\tabular{ll}{
   Field \tab Description \cr
   \code{pmed} \tab Point estimate of P_med \cr
   \code{pmed_ci_lower} \tab Lower bound of MBCO CI \cr
   \code{pmed_ci_upper} \tab Upper bound of MBCO CI \cr
   \code{pmed_p} \tab Two-sided p-value (H0: P_med ≤ 0.5) \cr
   \code{branch_switch} \tab 1 if the MBCO union-null LRT switched branches \cr
   \code{converged} \tab 1 if optimisation converged \cr
}


\strong{Algorithm:}
\enumerate{
\item Fit linear SEM: \code{M ~ A} and \code{Y ~ A + M}.
\item Estimate path coefficients α (A→M) and β (M→Y).
\item Estimate P_med via a parametric bootstrap PO draw under the estimated SEM.
\item Build a delta-method normal CI and clamp to [0, 1].
\item The "branch_switch" flag records whether the union-null test selected the
constrained branch (α·β ≤ 0) over the unconstrained branch (α·β > 0).
}
}
\seealso{
\code{\link[=medsim_scenario_pmed]{medsim_scenario_pmed()}}, \code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}}
}
