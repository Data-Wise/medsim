% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyze_missing.R
\name{medsim_summarize_branch_switch}
\alias{medsim_summarize_branch_switch}
\title{Summarize the MBCO branch-switch rate per scenario}
\usage{
medsim_summarize_branch_switch(results, by = "scenario")
}
\arguments{
\item{results}{A \code{medsim_results} object from \code{\link[=medsim_run]{medsim_run()}}.}

\item{by}{Grouping columns (default \code{"scenario"}).}
}
\value{
A data.frame: one row per group with \code{branch_switch_rate} and \code{n_valid}.
}
\description{
Reads the \code{branch_switch} column emitted by \code{\link[=medsim_method_mbco_mi]{medsim_method_mbco_mi()}} (the union-null /
ab=0 diagnostic from MEMO-MBCO-MI-derivation) and returns a per-scenario summary suitable
for joining as a column onto a \code{medsim_table_coverage()} table. Non-converged rows
(\code{converged == 0}) are dropped before summarizing.
}
