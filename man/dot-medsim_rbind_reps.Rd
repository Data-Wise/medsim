% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/runner.R
\name{.medsim_rbind_reps}
\alias{.medsim_rbind_reps}
\title{Schema-harmonized rbind of replication rows}
\usage{
.medsim_rbind_reps(rows)
}
\arguments{
\item{rows}{List of single-row (or multi-row) result data.frames.}
}
\value{
One combined data.frame (0-row frames are dropped).
}
\description{
Failed replications produce rows whose columns differ from success rows
(a failure row carries \code{error} but no estimate columns), and a bare
\code{do.call(rbind, ...)} crashes on the mismatch -- one transient rep failure
used to kill the entire scenario (chunked: the whole chunk file).

This helper unions the column sets, fills absent columns with \code{NA},
guarantees an \code{error} character column on EVERY row (\code{NA} on success), and
sets \code{converged = 0} on failure rows when a \code{converged} column exists --
so all rows share one schema and downstream code sees the documented
failure semantics instead of a crash.
}
\keyword{internal}
