% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{medsim_cache_load}
\alias{medsim_cache_load}
\title{Load Object from Cache}
\usage{
medsim_cache_load(file, check_expiry = FALSE, max_age = 30, verbose = TRUE)
}
\arguments{
\item{file}{Character: Path to cache file}

\item{check_expiry}{Logical: Check if cache has expired (default FALSE)}

\item{max_age}{Numeric: Maximum age of cache in days (default 30). Only
used if \code{check_expiry = TRUE}.}

\item{verbose}{Logical: Print messages about cache status (default TRUE)}
}
\value{
Cached object if file exists and is valid, NULL otherwise
}
\description{
Loads a previously cached R object. Returns NULL if cache file doesn't exist.
}
\details{
\subsection{Cache Validation}{

When loading, the cache is checked for:
\itemize{
\item File exists
\item File is readable
\item Age is within max_age (if check_expiry = TRUE)
}
}

\subsection{Handling Missing Cache}{

If cache doesn't exist:
\itemize{
\item Returns NULL (no error)
\item Calling code should compute and cache the result
}

Example pattern:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{result <- medsim_cache_load("cache/truth.rds")
if (is.null(result)) \{
  result <- expensive_computation()
  medsim_cache_save(result, "cache/truth.rds")
\}
}\if{html}{\out{</div>}}
}
}
\examples{
\dontrun{
# Load from cache
truth <- medsim_cache_load("cache/truth_scenario1.rds")

if (is.null(truth)) {
  # Cache miss - compute and cache
  truth <- compute_expensive_truth()
  medsim_cache_save(truth, "cache/truth_scenario1.rds")
}

# Load with expiry check
result <- medsim_cache_load(
  "cache/old_result.rds",
  check_expiry = TRUE,
  max_age = 7  # 7 days
)
}

}
\seealso{
\code{\link[=medsim_cache_save]{medsim_cache_save()}}, \code{\link[=medsim_cache_exists]{medsim_cache_exists()}}
}
