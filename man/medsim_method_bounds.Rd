% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_dm.R
\name{medsim_method_bounds}
\alias{medsim_method_bounds}
\title{Bounds estimator adapter for differential-misclassification (DM) studies}
\usage{
medsim_method_bounds(
  data,
  params,
  misclass_type = c("mediator", "exposure"),
  alpha = 0.05
)
}
\arguments{
\item{data}{A data.frame produced by the scenario's \code{data_generator}.
Expected columns: \code{A} (binary treatment), \code{M} or \code{M_star} (mediator,
possibly misclassified), \code{Y} (outcome).}

\item{params}{Named list of scenario parameters.  Must include \code{NDE} and
\code{NIE} (ground-truth natural effects) so truth can be tracked.}

\item{misclass_type}{\code{"mediator"} (default) or \code{"exposure"}.}

\item{alpha}{Nominal level; \code{1 - alpha} CI (default \code{0.05}).}
}
\value{
Named list with elements \code{NDE_lower}, \code{NDE_upper},
\code{NDE_im_lower}, \code{NDE_im_upper}, \code{NIE_lower}, \code{NIE_upper},
\code{NIE_im_lower}, \code{NIE_im_upper}, \code{feasible}, \code{falsified}.
}
\description{
Returns partial-identification bounds (NDE/NIE) following the \code{interval}-kind
result contract expected by \code{\link[=medsim_run]{medsim_run()}}, so that
\code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} dispatches the Imbens-Manski / partial-ID
interval coverage branch.

\strong{Current implementation:} OLS decomposition + inflated interval (synthetic
bounds for unit-testing the harness).  A future version will call
\code{medrobust::bound_ne()} once its accessor API stabilises on CRAN.

Result columns returned (flat named list):
\itemize{
\item \code{NDE_lower}, \code{NDE_upper} -- bounds on natural direct effect
\item \code{NDE_im_lower}, \code{NDE_im_upper} -- Imbens-Manski CI for NDE
\item \code{NIE_lower}, \code{NIE_upper} -- bounds on natural indirect effect
\item \code{NIE_im_lower}, \code{NIE_im_upper} -- Imbens-Manski CI for NIE
\item \code{feasible} -- logical: bounds are non-empty / consistent
\item \code{falsified} -- logical: data rejects identifying assumptions
}
}
\examples{
set.seed(42)
d <- data.frame(A = rbinom(200, 1, 0.5), M = rnorm(200), Y = rnorm(200))
result <- medsim_method_bounds(d, list(NDE = 0.2, NIE = 0.3))
str(result)

}
\seealso{
\code{\link[=medsim_scenario_dm]{medsim_scenario_dm()}} for the companion scenario factory;
\code{\link[=medsim_analyze_coverage]{medsim_analyze_coverage()}} for interval-kind coverage analysis.
}
