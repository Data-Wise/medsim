# Format Speedup for LaTeX

Format Speedup for LaTeX

## Usage

``` r
.format_speedup_latex(x, digits = 1)
```

## Arguments

- x:

  Speedup factor

- digits:

  Decimal places

## Value

Character string with times symbol
