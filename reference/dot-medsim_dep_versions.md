# Installed versions of medsim's key soft dependencies

Installed versions of medsim's key soft dependencies

## Usage

``` r
.medsim_dep_versions()
```

## Value

Named character vector (absent packages omitted).
